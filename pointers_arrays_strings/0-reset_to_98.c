#include "main.h"
#include <stdio.h>

/**
*
*main - reset and updates the value to 98
*
*Return always to 0 
*
*/
int main() 
{
int value = 10;
printf("n=%d\n", n);
reset_to_98(&n);
printf("n=%d\n", n);
return (0);
}
